<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.plucky">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Plucky">
        <activity android:name=".ActivityEsperar"></activity>
        <activity android:name=".ActivityRoom" />
        <activity android:name=".Juego" />
        <!--
 <activity
             android:name=".MenuNavegacion"
             android:label="@string/title_activity_menu_navegacion" />
         <activity
             android:name=".Navegacion"
             android:label="@string/title_activity_navegacion" />
        -->
        <activity android:name=".DetalleJugador" />
        <activity android:name=".EscenarioOnline" />
        <activity android:name=".Ajustes" />
        <activity android:name=".Ingreso" />
        <activity android:name=".Menu" />
        <activity android:name=".Registro" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>